# This module is now unused. All auto-scanning is controlled by the GUI (see gui.py).
# The scan interval and enable/disable are set by the user in the app.
# No background scheduler is started here.
